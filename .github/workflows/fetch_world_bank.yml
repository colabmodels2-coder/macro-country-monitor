name: Fetch World Bank Data

on:
  workflow_dispatch:
  schedule:
    - cron: "15 6 * * 1"

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  fetch-world-bank:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install requirements
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run World Bank fetch script
        run: |
          python scripts/fetch_world_bank.py

      - name: Rebuild master dataset
        run: |
          python scripts/build_master_dataset.py

      - name: Commit updated raw and processed data
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add data/raw/world_bank/world_bank_real.csv
          git add data/processed/master_dataset.csv
          git diff --cached --quiet || git commit -m "Update World Bank raw and processed data"
          git push
